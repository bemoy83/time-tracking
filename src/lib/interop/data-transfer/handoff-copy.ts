export const CANONICAL_HANDOFF_EXPLANATION =
  'Cross-device updates happen only through shared JSON handoff files.';

export const PLANNER_EXECUTION_RETURN_EXPLANATION =
  'Local/offline saves stay on the field device. "Pending sync" is not planner transfer. This plan updates here only after you import an execution return JSON file.';

export const FIELD_EXECUTION_RETURN_EXPLANATION =
  'Work stays on this device until you export and share an execution return JSON file with the planner.';
